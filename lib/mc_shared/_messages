% return if $m->request_comp->attr_exists('is_transient');
% if (exists $m->session->{messages}) {
%   foreach (@{ $m->session->{messages} }) {
<p id="message" class="message <% $_->[0] %>">\
	<% $_->[1] %>\
%		if (defined $_->[2] && $m->comp('/_is_member', group => 'developers')) {
	<h4>DEBUG</h4>\
	<div class="debug pre"><% $_->[2] |h %></div>\
%		}
</p>
%   }
%   delete $m->session->{messages};
% }

<%flags>
	inherit => undef
</%flags>
