{
	program => '/usr/local/RepeatMasker/RepeatMasker',
	options => [
				'-no_id',
				'-qq',					# about 10% less sensitive, 4->10 times faster than default
				'-engine', 'crossmatch',
				#'-gff',					# Creates an additional GFF format output
				'-nolow'
			],
	option_output_dir => '-dir',
	output_dir => 'output',		# must be the same with options->{-dir}

	species_map => {
		d => [qw/-species eudicotyledons/], #must be ARRAY ref
		m => [qw/-species monocotyledons/],
		default => [qw/-species viridiplantae/],
		a => [qw/-species bilateria/],
	},
}

