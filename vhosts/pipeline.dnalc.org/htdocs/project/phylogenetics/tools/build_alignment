{status:'<% $status|js%>', message:'<% $msg |js%>'}
<%args>
	$pid => 0
	$aln => ''
</%args>
<%once>
	use DNALC::Pipeline::App::Phylogenetics::ProjectManager ();
	#use DNALC::Pipeline::Phylogenetics::Pair ();
</%once>
<%init>
	my ($status, $msg) = ("error", "");
	my $pm = DNALC::Pipeline::App::Phylogenetics::ProjectManager->new($pid);
	my $proj = $pm->project;
	unless ($proj) {
		$msg = "Project not found."
	}
	else {
		if ($aln) {
			#//document.applets.Jalview.getSelectedSequencesAsAlignment("fasta", false)
			# parse $aln, extract the seq_ids and make sure they belong to this project
			
		}
		else {
			if ($pm->build_alignment) {
				$status = "success";
			}
		}
		#$status = "success";
	}
</%init>

<%flags>
	inherit => undef
</%flags>