<div id="topnavi">
	<a href="/">Home</a>
	<a href="/project/">Project selector</a>
% if ($s->{logged_in}) {
		<a href="/logout">Logout</a>
% } else {
		<a href="/">Login</a>
% }
</div>
% $m->call_next;
<%init>
	$m->session->{pipeline} ||= {};
	my $s = $m->session->{pipeline};
	my $now = time();

	if ($s->{logged_in}) {
	}
	else {
		#pretend the user logged in already
		$s->{user_id}  = 45;
		$s->{username} = "guest";
	}
	$s->{last} = $now;
	
</%init>

%# vim: ft=mason
